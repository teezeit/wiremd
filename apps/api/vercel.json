{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "outputDirectory": ".",
  "ignoreCommand": "git -C $(git rev-parse --show-toplevel) diff --quiet HEAD^ HEAD -- apps/api packages/",
  "functions": {
    "api/[[...route]].ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/[[...route]]" }]
}
