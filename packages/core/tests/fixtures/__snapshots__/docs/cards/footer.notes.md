# Design question: why does `footer` exist as a separate container?

The `footer` container type is implemented but it's unclear from the
demo why it exists vs `section`. They appear to render identically.

**Type:** design clarity.
**Action:** either differentiate `footer` visually (smaller text,
muted background, top border) or document that it's a semantic-only
wrapper.
