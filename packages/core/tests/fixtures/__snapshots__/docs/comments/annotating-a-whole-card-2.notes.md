# Demo title should read "annotating an item inside a card"

The demo title in `apps/docs/components/comments.md` for this fixture
duplicates the previous one (`annotating-a-whole-card`). It should be
renamed in the docs to something like "annotating an item inside a
card" to disambiguate.

**Type:** docs naming.
**Action:** rename the `## Annotating a whole card` heading (the second
one) in `apps/docs/components/comments.md` to a distinct title. Will
auto-update the fixture name.
