<!-- regression: same scenario as blank-line-before-list, but the blank line
     is omitted. Documents the (currently buggy) behavior so future fixes
     surface as a snapshot diff rather than silent regression. -->
::: card
- item one
- item two
:::
