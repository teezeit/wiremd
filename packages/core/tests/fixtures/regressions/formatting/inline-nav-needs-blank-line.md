<!-- regression: `[[ a > b > c ]]` breadcrumb syntax requires a preceding
     blank line. Without one, the line above absorbs the opening `[[` and
     the breadcrumb renders as a button labelled `[ a > b > c ]` instead. -->
::: card
some intro text
[[ home > docs > components ]]

:::
